
// Initial State
const initialState = { token: '' };

const AppReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

// Export Reducer
export default AppReducer;
